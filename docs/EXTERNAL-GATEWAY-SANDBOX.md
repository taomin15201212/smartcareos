# M14 外部渠道沙箱

通知与政务交换新增 `dispatch` 动作。默认 `sandbox` 模式生成本地外部流水号并完成状态闭环；`http` 模式向配置端点发送 JSON，并按 HTTP 2xx 判断接受结果。

真实微信、短信和政务环境需要各渠道凭据、签名/加密规则、IP 白名单与正式数据契约。当前交付完成适配边界和本地沙箱，不声称通过第三方验收。

## 来源事实与架构推演

- 来源事实：家属小程序和政务接口来自招聘信息。
- 架构推演：统一外部网关、沙箱流水号和 HTTP 适配协议为当前工程设计。
