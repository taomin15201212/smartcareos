# 通知与政务交换

## 来源事实

产品边界包含家属微信小程序和政务监管接口。

## 架构推演

通知仅存业务引用、最小摘要、收件人与 `PENDING -> SENT|FAILED` 结果。政务交换保存合同、映射版本、期间、载荷哈希与 `PENDING -> SUBMITTED -> ACCEPTED|REJECTED` 回执。

真实通道与监管端点必须通过独立适配器接入，不得将外部响应直接写入老人或告警主表。
